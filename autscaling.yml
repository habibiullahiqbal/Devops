#frontend 
name: production

# on:
#   push:
#     branches: [main]

# permissions:
#   contents: read
#   id-token: write

# jobs:
#   build-and-push:
#     name: Build and Push Docker Image to ECR
#     runs-on: ubuntu-latest
#     environment: prod

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           audience: sts.amazonaws.com
#           role-to-assume: ${{ secrets.PROD_OIDC_ROLE_ARN }}
#           aws-region: ${{ secrets.AWS_REGION }}

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Build and Push Frontend
#         run: |
#           docker build \
#             --build-arg VITE_API_URL="${{ secrets.VITE_API_URL }}" \
#             --build-arg VITE_APP_ENV="${{ secrets.VITE_APP_ENV }}" \
#             --build-arg VITE_ENABLE_ANALYTICS="${{ secrets.VITE_ENABLE_ANALYTICS }}" \
#             -t ${{ secrets.FRONTEND_ECR_URL }}:latest .
#           docker push ${{ secrets.FRONTEND_ECR_URL }}:latest

#   continuous-deployment:
#     name: Blue Green Deployment
#     runs-on: ubuntu-latest
#     environment: prod
#     needs: build-and-push

#     steps:
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           audience: sts.amazonaws.com
#           role-to-assume: ${{ secrets.PROD_OIDC_ROLE_ARN }}
#           aws-region: ${{ secrets.AWS_REGION }}

#       - name: Start hard instance refresh
#         id: refresh
#         run: |
#           REFRESH_ID=$(aws autoscaling start-instance-refresh \
#             --auto-scaling-group-name "${{ secrets.PROD_ASG_NAME }}" \
#             --strategy Rolling \
#             --preferences '{
#               "MinHealthyPercentage": 75,
#               "MaxHealthyPercentage": 100,
#               "InstanceWarmup": 300,
#               "SkipMatching": false
#             }' \
#             --query 'InstanceRefreshId' \
#             --output text)

#           echo "REFRESH_ID=$REFRESH_ID" >> $GITHUB_ENV
#           echo "Started refresh: $REFRESH_ID"

#       - name: Wait for refresh to complete
#         run: |
#           while true; do
#             STATUS=$(aws autoscaling describe-instance-refreshes \
#               --auto-scaling-group-name "${{ secrets.PROD_ASG_NAME }}" \
#               --instance-refresh-ids "$REFRESH_ID" \
#               --query 'InstanceRefreshes[0].Status' \
#               --output text)

#             echo "Current status: $STATUS"

#             if [ "$STATUS" = "Successful" ]; then
#               echo "Instance refresh completed successfully"
#               exit 0
#             fi

#             if [ "$STATUS" = "Failed" ] || [ "$STATUS" = "Cancelled" ]; then
#               echo "Instance refresh failed"
#               exit 1
#             fi

#             sleep 30
#           done




#-----------backend---------------

name: Production backend deployment

# on:
#   push:
#     branches: [main]

# permissions:
#     id-token: write
#     contents: read

# jobs:
#   build-and-push:
#     name: Build and push Docker Image to ECR
#     runs-on: ubuntu-latest
#     environment: production

#     steps:
#       - name: checkout code
#         uses: actions/checkout@v4
  
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           audience: sts.amazonaws.com
#           role-to-assume: ${{ secrets.PROD_OIDC_ROLE_ARN }}
#           aws-region: ${{ secrets.AWS_REGION }}

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Build image
#         run: |
#           docker build -t prod-backend .

#       - name: Tag image
#         run: |
#           docker tag ${{ secrets.PROD_BACKEND_ECR_REPOSITORY }}:latest ${{ secrets.PROD_BACKEND_ECR_URL }}:latest
#       - name: Push image
#         run: |
#           docker push ${{ secrets.PROD_BACKEND_ECR_URL }}:latest

# #---------------deployment job-----------------

#   continuous_deployment:
#     name: Hard Refresh Auto Scaling Group
#     runs-on: ubuntu-latest
#     environment: production
#     needs: build-and-push

#     steps:
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           audience: sts.amazonaws.com
#           role-to-assume: ${{ secrets.PROD_OIDC_ROLE_ARN }}
#           aws-region: ${{ secrets.AWS_REGION }}
          

#       - name: Start instance refresh
#         id: refresh
#         run: |
#           REFRESH_ID=$(aws autoscaling start-instance-refresh \
#             --auto-scaling-group-name "${{ secrets.PROD_ASG_NAME }}" \
#             --strategy Rolling \
#             --preferences '{
#               "MinHealthyPercentage": 75,
#               "MaxHealthyPercentage": 100,
#               "InstanceWarmup": 300,
#               "SkipMatching": false
#             }' \
#             --query 'InstanceRefreshId' \
#             --output text)

#           echo "REFRESH_ID=$REFRESH_ID" >> $GITHUB_ENV
#           echo "Started refresh: $REFRESH_ID"

#       - name: Wait for refresh to complete
#         run: |
#           while true; do
#             STATUS=$(aws autoscaling describe-instance-refreshes \
#               --auto-scaling-group-name "${{ secrets.PROD_ASG_NAME }}" \
#               --instance-refresh-ids "$REFRESH_ID" \
#               --query 'InstanceRefreshes[0].Status' \
#               --output text)

#             echo "Current status: $STATUS"

#             if [ "$STATUS" = "Successful" ]; then
#               echo "Instance refresh completed successfully"
#               exit 0
#             fi

#             if [ "$STATUS" = "Failed" ] || [ "$STATUS" = "Cancelled" ]; then
#               echo "Instance refresh failed"
#               exit 1
#             fi

#             sleep 30
#           done
