 
###---------------------Nginx setup in ec2 ----------------------------
 
 sudo apt update
      sudo apt-get install nginx -y
      sudo systemctl start nginx
      sudo systemctl enable nginx
      sudo systemctl status nginx
      cd /etc/nginx/ 
      cd sites-available
      sudo nano uatapp.pharmalyfe.com.conf

sudo ln -s /etc/nginx/sites-available/pharmalyfe.com.conf /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx


       Nginx files content check # nginx -T

Cerbot install command
#	           sudo snap install --classic certbot
Create a symlink:	
#	           sudo ln -s /snap/bin/certbot /usr/bin/certbot
#	           certbot --version
Nginx plugin	
#	           sudo certbot --nginx

Email add
Enter domain number
Certificate
Successfully received certificate.



 #          sudo certbot  renew --dry--nginx









#------------------------------------- DataBase setup---------------------------------


mysqldump -h pharma-database.cvo4k8c8woz3.eu-west-2.rds.amazonaws.com -u admin	 -p pharmalyfenew > pharmalyfenew_dump.sql							
enter database password

#------Create a New Database (pharmalyfeUET) on RDS						
mysql -h pharma-database.cvo4k8c8woz3.eu-west-2.rds.amazonaws.com -u admin -p

#--------Once connected, create the new database:							
CREATE DATABASE pharmalyfeUAT;								

#-----------Restore the Dump into the New Database:
mysql -h pharma-database.cvo4k8c8woz3.eu-west-2.rds.amazonaws.com -u admin -p pharmalyfeUAT < pharmalyfenew_dump.sql	

#----------------------Open database	---------------------------									
mysql -h pharma-database.cvo4k8c8woz3.eu-west-2.rds.amazonaws.com -u admin -p		show databases;											
use pharmalyfeUAT;	
#------------------------Delete data base
Delete database # 
drop database pharmalyfeUET;










